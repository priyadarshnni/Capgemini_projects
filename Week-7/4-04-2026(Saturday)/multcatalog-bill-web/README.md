# multcatalog-bill-web (React / CRA)

Project documentation — architecture, API endpoints, data flow, and run instructions — lives in the parent folder:

**[../README.md](../README.md)**

This app is plain **JavaScript** + **Create React App**. Source lives under `src/`; see file-top comments in `api.js`, `billUtils.js`, and `App.jsx` for how each area works.
